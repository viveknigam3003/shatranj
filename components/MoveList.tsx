import { Box } from "@chakra-ui/react";
import React from "react";

interface MoveListProps {
  pgn: string;
}

const MoveList: React.FC<MoveListProps> = ({ pgn }) => {
  return <Box>

  </Box>;
};

export default MoveList;
